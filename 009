class Solution {
public:
    bool isPalindrome(int x) {
        if(x<0)return false;
        
        int res=0,n=x;
        
        while(n){
            res=res*10+n%10;
            n/=10;
        }
        return res==x;
    }
};
